#!/usr/bin/env node

var argv = require('optimist').argv;
var jsbundle = require('../jsbundle');

var config = jsbundle.parseConfig(argv.config, null, argv.env || process.env.JSBUNDLE_ENV);

jsbundle.devCdn(argv.port || 8081, config);

